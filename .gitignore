# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg

# Virtual envs
.venv/
venv/
env/

# Environment
.env
.env.local
.env.*.local

# Logs (production logs are generated, not committed; fixtures are committed)
ai/week1-rag/logs/*.jsonl
!ai/week1-rag/logs/.gitkeep

# Monitoring DBs (runtime-generated — recomputed from logs + rate-limiter state)
monitoring/feedback.db
monitoring/feedback.db-journal
monitoring/rate_limit.db
monitoring/rate_limit.db-journal

# Streamlit cache
.streamlit/

# IDE
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Data — demo corpus IS committed (pre-baked), but drop large binaries if they sneak in
data/rag/**/embeddings/*.npy
!data/rag/demo/**/embeddings/*.npy

# Notebook checkpoints
.ipynb_checkpoints/

# Scorer output is committed (placeholder + regenerated by scorer/run_self_score.sh).
# The pinned scorer clone goes here instead:
scorer/_clone/
.mutmut_cache/
